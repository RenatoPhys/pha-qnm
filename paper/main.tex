\documentclass[11pt,a4paper]{article}
\ifdefined\XeTeXversion\else
  \pdfoutput=1
\fi
\usepackage{jheppub}
\usepackage{bm}
\usepackage{booktabs}
\usepackage{flafter}
\usepackage{placeins}

\setlength{\emergencystretch}{2em}

\newcommand{\dd}{\mathrm{d}}
\newcommand{\ii}{\mathrm{i}}
\newcommand{\mub}{\mu_B}
\newcommand{\rhob}{\rho_B}
\newcommand{\w}{\mathfrak{w}}
\newcommand{\q}{\mathfrak{q}}
\newcommand{\order}{\mathcal{O}}

\title{Critical relaxation and spinodal instabilities\\
in Bayesian holographic QCD}

% Author, affiliation, email, ORCID, funding, and arXiv metadata must be
% supplied by the authors before submission. They are intentionally not
% replaced by fictitious metadata in this working manuscript.

\abstract{%
We compute the low-lying real-time spectrum of a Bayesian-calibrated
Einstein--Maxwell--dilaton model across its finite-density phase structure.
Coupled helicity-one and helicity-zero quasinormal modes reproduce
charged-hydrodynamic predictions obtained independently from the equation of
state and horizon transport.  Shooting and multidomain Chebyshev integration
agree, with source and constraint residuals monitored for every accepted root.
At the maximum-likelihood critical endpoint,
the baryon/heat-diffusion coefficient vanishes as the susceptibility diverges.
The thermodynamic approach gives $z=4.08\pm0.05$, dominated by scaling-window
variation, while the critical QNM directly exhibits quartic small-momentum
damping.  Along the spinodal branch at $\mub=650$ MeV, a dedicated mode-identity
audit shows that the sound pair remains stable and the charged diffusive branch
alone crosses into the upper half frequency plane.  Full QNMs close this
unstable band; at its midpoint $\q_*=0.283$, the maximum dimensionless growth
is $1.41\times10^{-3}$, and $\q_{\rm edge}=0.417$.  Posterior propagation gives
$4.062<z<4.085$ at 95\% credibility before the common fit-window systematic,
and every representative posterior model retains the diffusive spinodal
instability.  These results provide a numerically cross-validated
characterization of critical slowing and spinodal growth in a
lattice-constrained holographic plasma; they describe the calibrated model,
not a direct determination of QCD dynamics.}

\keywords{Gauge-Gravity Correspondence, Non-Equilibrium Field Theory,
Finite Temperature or Finite Density, Quark-Gluon Plasma}

\hypersetup{
  pdftitle={Critical relaxation and spinodal instabilities in Bayesian holographic QCD},
  pdfsubject={Coupled quasinormal modes, critical slowing, spinodal growth, and posterior uncertainty in a calibrated EMD model},
  pdfkeywords={Gauge-Gravity Correspondence, Non-Equilibrium Field Theory, Finite Temperature or Finite Density, Quark-Gluon Plasma}
}

\begin{document}
\maketitle
\flushbottom

\section{Introduction}
\label{sec:introduction}

The real-time response of strongly interacting matter at nonzero baryon
density is difficult to determine from first principles.  Lattice QCD
constrains equilibrium thermodynamics near vanishing chemical potential, but
the sign problem obstructs direct access to the dense phase diagram and
Minkowski observables.  Bottom-up Einstein--Maxwell--dilaton (EMD) models offer
a complementary laboratory in which a lattice-constrained equation of state,
transport, and retarded response follow from the same black-brane solution.

A critical endpoint is a dynamical as well as a thermodynamic object.
Conserved densities, their mode mixing, and the divergence of static
susceptibilities determine critical slowing down.  The QCD critical endpoint
is expected to exhibit Model-H dynamics \cite{Hohenberg:1977ym,Son:2004iv},
whereas classical large-$N_c$ holography suppresses some nonlinear mode
couplings and can realize a different effective exponent
\cite{DeWolfe:2011ts,Roth:2024hpn}.  The exponent and the pole responsible for it must
therefore be extracted from the real-time spectrum rather than inferred from
the static equation of state.

The metastable branches of a first-order transition pose a second dynamical
question.  Between the two spinodal boundaries a thermodynamic curvature
changes sign, and a longitudinal hydrodynamic mode becomes unstable.  Full
gravitational dynamics must determine whether the instability occupies a
finite momentum band, its fastest-growing wavelength, and the range over
which a gradient expansion remains quantitative.  Holographic spinodal modes
have been studied in Einstein--scalar models
\cite{Janik:2015waa,Janik:2016btb,Caddeo:2024qgm}; the corresponding charged,
lattice-calibrated EMD calculation requires the complete helicity-zero system.

The PHA analysis of ref.~\cite{Hippert:2023bel} calibrated polynomial--
hyperbolic EMD models to lattice data and released the posterior ensemble
\cite{HippertZenodo}.  Homogeneous nonhydrodynamic QNMs were previously
studied in a different QCD-like EMD model \cite{Rougemont:2018taa}, and recent
work propagates the PHA posterior to equilibrium and horizon transport
\cite{Khan:2026uq}.  These results do not determine the coupled finite-
momentum pole structure, the critical diffusion law, or the full spinodal
band.  Charged-fluid studies further show that mode mixing and pole collisions
can sharply limit hydrodynamics \cite{Abbasi:2020ykq,Jansen:2020hfd}.
Recent improved-holographic-QCD, V-QCD, and Einstein--dilaton calculations
further illustrate the range of finite-density instabilities and channel
spectra \cite{Alho:2020gwl,CruzRojas:2024lwo,Villarreal:2025qtm}.

This work closes that gap.  We first reconstruct the horizon-data surface,
continue fixed-$\mub$ branches through both folds, and independently locate
the cusp of the thermodynamic map.  We then solve the coupled helicity-one and
helicity-zero perturbations in ingoing Eddington--Finkelstein coordinates.
Gauge-invariant sources are evaluated on a physical horizon basis after
quotienting exact residual-gauge solutions.  Adaptive DOP853 shooting and an
independent multidomain Chebyshev integration provide two radial methods.

On stable backgrounds, the resulting shear, sound, and baryon/heat-diffusion
poles reproduce a separately constructed charged-hydrodynamic evolution
matrix.  Along a fixed-$\mub=\mub^c$ approach, diffusion vanishes and the
susceptibility diverges; the inferred exponent is tested directly through
the critical QNM dispersion.  Inside the spinodal region, the full
helicity-zero pole is followed beyond the range where hydrodynamics predicts
unbounded $q^2$ growth, revealing a finite unstable band.

Finally, we propagate uncertainty through 25 weighted posterior medoids.  The
medoids are selected in joint parameter/critical-point space, include the
maximum-likelihood draw, and represent all 1589 posterior samples whose
released critical-point calculation succeeded.  For every medoid we
independently reconstruct the cusp, approach it along a physical path, and
locate both spinodals at $\mub^c+50$ MeV.  We keep numerical, scaling-window,
and posterior uncertainties separate.

The main advances are threefold.  First, we obtain the coupled finite-$\q$
spectrum in the calibrated PHA realization and validate every hydrodynamic
branch against an independently constructed charged-fluid matrix.  Second,
we extract the model's critical exponent both from thermodynamic scaling and
directly from the critical QNM.  Third, we demonstrate full-QNM closure of the
spinodal band, identify its carrier as the baryon/heat-diffusion branch rather
than the stable sound pair, and show that the existence of this instability
survives posterior propagation.

Section~\ref{sec:model} defines the calibrated model and phase structure.
Sections~\ref{sec:perturbations} and \ref{sec:numerics} give the coupled
systems and validation.  Stable hydrodynamics, critical dynamics, spinodal
growth, and posterior propagation are presented in
sections~\ref{sec:stable}--\ref{sec:posterior}.  Appendices collect symbolic
identities, the error budget, background and thermodynamic reproduction,
homogeneous trajectories and the 2018-model benchmark, and complete
reproducibility details.

\section{PHA EMD model and phase structure}
\label{sec:model}

We use natural units, mostly-plus signature, and set the asymptotic AdS radius
to one.  The action is
\begin{equation}
 S=\frac{1}{2\kappa_5^2}\int \dd^5x\sqrt{-g}\left[
 R-\frac12(\partial\phi)^2-V(\phi)-\frac14 f(\phi)F_{MN}F^{MN}\right].
 \label{eq:action}
\end{equation}
The polynomial--hyperbolic parametrization is
\begin{align}
 V(\phi)&=-12\cosh(\gamma\phi)+b_2\phi^2+b_4\phi^4+b_6\phi^6,\nonumber\\
 f(\phi)&=\frac{\operatorname{sech}g(\phi)
 +d_1\operatorname{sech}(d_2\phi)}{1+d_1},
 &g(\phi)&=c_1\phi+c_2\phi^2+c_3\phi^3.
 \label{eq:pha}
\end{align}
The maximum-likelihood realization is sample 74 of the public posterior.  Its
exact binary64 parameters are stored in the machine-readable configuration;
the rounded values most relevant here are
\begin{align}
 \Lambda&=1123.870\ {\rm MeV},& \kappa_5^2&=11.37047,&
 \gamma&=0.590424,\nonumber\\
 (b_2,b_4,b_6)&=(0.327227,-0.0480134,0.000938515).&&&
\end{align}
The Maxwell parameters are
$(c_1,c_2,c_3)=(0.0088005,0.155799,0.0528185)$ and
$(d_1,d_2)=(1.71775,1679.18)$.  The large $d_2$ creates a narrow UV structure
in $f(\phi)$ that is retained in all backgrounds and perturbation kernels.

In domain-wall gauge,
\begin{equation}
 \dd s^2=e^{2A(r)}[-h(r)\dd t^2+\dd\bm{x}^2]+\frac{\dd r^2}{h(r)},
 \qquad A_M\dd x^M=\Phi(r)\dd t .
 \label{eq:background}
\end{equation}
We choose $r_H=A_H=h_H=\Phi_H=0$ and $h'_H=1$.  The independent horizon data
are $(\phi_0,\Phi_1)$, with
$|\Phi_1|<[-2V(\phi_0)/f(\phi_0)]^{1/2}$.  The conserved flux
$\mathcal Q=e^{2A}f\Phi'$ is evolved analytically, and the UV coefficients give
\begin{align}
 T&=\frac{\Lambda}{4\pi\phi_A^{1/\nu}\sqrt{h_0}},&
 \mub&=\frac{\Phi_0\Lambda}{\phi_A^{1/\nu}\sqrt{h_0}},\nonumber\\
 s&=\frac{2\pi\Lambda^3}{\kappa_5^2\phi_A^{3/\nu}},&
 \rhob&=-\frac{\Phi_2\Lambda^3}
 {\kappa_5^2\phi_A^{3/\nu}\sqrt{h_0}},
 \label{eq:thermodynamics}
\end{align}
where $\nu=4-\Delta$ and $\Delta=2+\sqrt{4+m^2}$.

\subsection{Cusp, coexistence, and spinodals}

Continuation is performed in horizon data rather than by isolated inversion
in $(T,\mub)$.  Folds satisfy
\begin{equation}
 \det\frac{\partial(\log T,\log\mub)}
 {\partial(\phi_0,\Phi_1/\Phi_1^{\rm max})}=0.
 \label{eq:fold}
\end{equation}
At the cusp, the derivative of this determinant along the null direction of
the same Jacobian also vanishes.  Along fixed chemical potential, pressure
differences follow from $\dd P=s\,\dd T$; equal pressure selects coexistence.

The $141\times121$ MAP horizon surface resolves 65 fixed-$\mub$ lines from
594 to 850 MeV.  Grid coarsening changes spinodal temperatures by at most
0.018 MeV and coexistence temperatures by at most 0.0085 MeV.  The independent
cusp is
\begin{equation}
 (T_c,\mub^c)=(103.7553,593.3227)\ {\rm MeV},\qquad
 (\phi_0,\Phi_1/\Phi_1^{\rm max})=(3.528281,0.346374).
 \label{eq:cusp}
\end{equation}
An independent 800-line crossing construction gives $(103.7611,593.3845)$
MeV.  The HDF5 metadata for sample 74 instead store
$(103.8978,602.4749)$ MeV.  Exact parameters and the reference UV prescription
were used, so the 9.152 MeV chemical-potential offset is retained as a
provenance discrepancy; the local cusp in eq.~\eqref{eq:cusp} defines the
dynamical background.

\begin{figure}[t]
 \centering
 \includegraphics[width=\textwidth]{figures/phase_diagram.pdf}
 \caption{MAP phase structure and dynamical paths.  The blue curve is
 equal-pressure coexistence and the dashed vermillion curves are the hot and
 cold folds; their light envelope denotes the region in which stable,
 metastable, and mechanically unstable branches coexist, not a uniformly
 unstable area.  The green star is the local CEP, while the gray cross is the
 distinct HDF5 metadata value; the inset also shows the converged crossing
 construction.  The green arrow gives the hot stable critical approach.  The
 vertical $\mub=650$ MeV cut and its three open symbols identify the
 backgrounds used for spinodal QNMs.}
 \label{fig:phase-diagram}
\end{figure}

Figure~\ref{fig:phase-diagram} shows the resulting branch-resolved phase
diagram.  Stable, metastable, and unstable states used below are selected on
these fixed-$\mub$ continuations, not on a fixed-horizon scan.

\FloatBarrier
\section{Coupled perturbations and charged hydrodynamics}
\label{sec:perturbations}

We transform eq.~\eqref{eq:background} to ingoing Eddington--Finkelstein
coordinates,
\begin{equation}
 \dd s^2=-e^{2A}h\,\dd v^2+2e^A\dd v\dd r+e^{2A}\dd\bm{x}^2,
 \qquad A=\Phi(r)\dd v,
\end{equation}
and use $e^{-\ii\omega v+\ii kz}$.  Stable poles have
$\operatorname{Im}\omega<0$.  We report
$\w=\omega/(2\pi T)$ and $\q=k/(2\pi T)$; the horizon-normalized solver uses
$\w=2\omega_{\rm num}$ and $k_{\rm num}=\q/(2\sqrt{h_0})$.

At finite momentum the fluctuations split into $SO(2)$ helicities.  Helicity
two is the decoupled tensor field.  The helicity-one primitives are
$(H_{vx},H_{zx},a_x)$, while helicity zero contains
\begin{equation}
 (H_{vv},H_{vz},H_{zz},H_{aa},a_v,a_z,\varphi),\qquad
 H_{aa}=H_{xx}=H_{yy}.
\end{equation}
The complete linearized Einstein, Maxwell, and scalar equations are generated
from eq.~\eqref{eq:action}.  They remain linear in
$\sigma=-\ii\omega$ and use $p=\ii k$.

\subsection{Gauge invariants and source matrix}

In helicity one the physical combinations are
\begin{equation}
 Z_{\rm sh}=pH_{vx}-\sigma H_{zx},\qquad Z_J=a_x.
 \label{eq:h1-invariants}
\end{equation}
For helicity zero we use
\begin{align}
 Z_H={}&p^2H_{vv}-2p\sigma H_{vz}+\sigma^2H_{zz}
 +\left[p^2\left(h+\frac{h'}{2A'}\right)-\sigma^2\right]H_{aa},\nonumber\\
 Z_A={}&p a_v-\sigma a_z-\frac{p\Phi'}{2A'}H_{aa},\qquad
 Z_\phi=\varphi-\frac{\phi'}{2A'}H_{aa}.
 \label{eq:h0-invariants}
\end{align}
These combinations annihilate the exact residual radial-gauge orbits.  At a
trial frequency, regular horizon data span seven and three solutions in
helicities zero and one, respectively.  Quotienting four and one analytic
gauge solutions leaves three and two physical solutions.  Their UV source
coefficients form square matrices $S_0(\omega,k)$ and $S_1(\omega,k)$; QNMs
satisfy
\begin{equation}
 \det S_s(\omega,k)=0,
 \label{eq:source-matrix}
\end{equation}
or, equivalently, a vanishing smallest singular value.  This is the standard
coupled-field source-matrix construction
\cite{Kovtun:2005ev,Kaminski:2009dh}.

Metric, Maxwell, and scalar source/vev powers are respectively $u^0/u^4$,
$u^0/u^2$, and $u^\nu/u^\Delta$ for $u=e^{-A}$.  We extract each source by a
UV value/derivative combination rather than fitting an exponentially small
difference.  Primitive radial Einstein and Maxwell equations not used for
evolution are evaluated as constraints on the physical source-null solution.

\subsection{Independent hydrodynamic prediction}
\label{sec:hydro-formulas}

Local derivatives of $(T,\mub,s,\rhob)$ with respect to the two horizon data
are transformed to derivatives in $(\epsilon,\rhob)$.  The Maxwell relation
$\left.\partial s/\partial\mub\right|_T=
\left.\partial\rhob/\partial T\right|_{\mub}$ provides an independent check.
We use $\eta=s/(4\pi)$ and the charged Eling--Oz horizon
formula \cite{Eling:2011ct}
\begin{equation}
 \frac{\zeta}{\eta}=\left(s\frac{\partial\phi_H}{\partial s}
 +\rhob\frac{\partial\phi_H}{\partial\rhob}\right)^2.
 \label{eq:eling-oz}
\end{equation}
The finite incoherent conductivity of the translationally invariant EMD
plasma is \cite{Zhou:2015qui}
\begin{equation}
 \frac{\sigma_Q}{T}=\frac{2\pi\sqrt{h_0}\,f(\phi_H)}{\kappa_5^2}
 \left(\frac{Ts}{\epsilon+P}\right)^2.
 \label{eq:sigmaQ}
\end{equation}
Rather than using $D_B=\sigma/\chi$ outside its neutral limit, we diagonalize
the full linearized charged-fluid evolution matrix for
$(\delta\epsilon,\delta\rhob,\delta u_z)$.  It gives
\begin{align}
 \w_{\rm sh}&=-\ii D_\eta\q^2+\order(\q^4),\nonumber\\
 \w_\pm&=\pm c_s\q-\ii\Gamma_s\q^2+\order(\q^3),\nonumber\\
 \w_D&=-\ii D_B\q^2+\order(\q^4),
 \label{eq:hydro-dispersions}
\end{align}
including charge/heat mixing.  All coefficients compared with QNMs below are
computed from equilibrium and horizon data before any dispersion fit.

\section{Numerical method and validation}
\label{sec:numerics}

Backgrounds are integrated from a second-order regular horizon series with
DOP853, relative tolerance $3\times10^{-12}$, absolute tolerance
$3\times10^{-14}$, and maximum step 0.025.  The conserved electric flux is
used directly, and UV coefficients are extracted on a fixed sampling grid.
Across the production phase surface, normalized background constraints stay
below $4.8\times10^{-9}$ and Gauss drift below $9.0\times10^{-8}$.

For coupled QNMs, the characteristic horizon equations and the radial Maxwell
constraint determine the regular first derivative.  Differentiating the six
characteristic helicity-zero equations once and adding the noncharacteristic
Maxwell equation fixes the second horizon coefficient.  Including this term
is essential: a first-order start excites a constraint-violating DAE solution,
whereas the second-order recurrence makes primitive constraints converge with
the horizon start.

The primary source-matrix solver integrates the physical horizon basis with
DOP853 from $r_0=10^{-6}$ to $r_{\rm UV}=5$.  The independent method converts
the same second-order system to first order and solves it on eight Chebyshev
subdomains separated at
\begin{equation}
 r=(10^{-4},10^{-3},10^{-2},10^{-1},0.5,1.5,3),
\end{equation}
using 24 intervals per domain.  It neither calls an ODE integrator nor forms
the global primitive QZ pencil.  Resolution, UV radius, horizon start, and the
subdomain partition are varied independently.

\begin{table}[t]
 \centering
 \caption{Acceptance summary for six neutral/charged hydrodynamic benchmark
 poles.  The continuum envelope includes $N=20,24,28$ per subdomain,
 $r_0=(0.5,1,2)\times10^{-6}$, $r_{\rm UV}=4.5,5.0,5.5$, and an independent
 partition.}
 \label{tab:coupled-validation}
 \begin{tabular}{@{}lr@{}}
 \toprule
 diagnostic & maximum or envelope\\
 \midrule
 symbolic EMD/gauge/constraint tests & $10/10$ passed\\
 horizon residual-gauge defect & $4.1\times10^{-16}$\\
 normalized source determinant & $1.6\times10^{-12}$\\
 primitive constraint, nominal & $9.0\times10^{-6}$\\
 primitive constraint, full $r_0$ study & $2.41\times10^{-5}$\\
 shooting--collocation distance in $\w$ & $3.7\times10^{-12}$\\
 continuum envelope in $\w$ & $3.7\times10^{-11}$\\
 hydro/QNM difference at $\q=0.05$ & $1.7\times10^{-3}$ relative\\
 \bottomrule
 \end{tabular}
\end{table}

The six benchmarks in table~\ref{tab:coupled-validation} comprise shear,
sound, and diffusion on a neutral background and on a charged stable
background.  The deliberately coarse $r_0=2\times10^{-6}$ variant has the
largest constraint; halving $r_0$ lowers it while changing $\w$ by only
$\order(10^{-11})$.  The MAP critical diffusion pole and central spinodal
maximum are also recomputed by shooting and by a 28-interval refined
collocation.  Their respective method distances are
$3.1\times10^{-11}$ and $1.5\times10^{-10}$.

The rejected global primitive helicity-zero QZ pencil has eigenvalue condition
numbers as large as $10^{15}$ and does not satisfy continuum constraints.  It
is not used for a frequency, an uncertainty, or a claim.  This exclusion is
important for a non-normal problem in which small discrete residuals alone do
not guarantee spectral stability \cite{Jaramillo:2020tuu,Destounis:2021lum}.

\section{Stable-phase hydrodynamic poles}
\label{sec:stable}

We first test the coupled calculation away from criticality.  Eight momenta
$0.02\leq\q\leq0.12$ are used on a neutral state at $T=282.77$ MeV and a
charged state at $(T,\mub)=(174.245,348.490)$ MeV.  Fits include the first
subleading power in $\q$ and extract the $\q\to0$ coefficient.

\begin{table}[t]
 \centering
 \caption{Independent hydrodynamic coefficients and their QNM small-momentum
 fits.  The sound attenuation is defined by
 $\operatorname{Im}\w_+=-\Gamma_s\q^2+\cdots$.}
 \label{tab:hydro}
 \begin{tabular}{@{}llrr@{}}
 \toprule
 state & coefficient & horizon/EOS & QNM fit\\
 \midrule
 neutral & $D_\eta$ & 0.500000 & 0.499998\\
         & $c_s$ & 0.532966 & 0.532966\\
         & $D_B$ & 0.998757 & 0.998671\\
         & $\Gamma_s$ & 0.375932 & 0.375933\\
 \addlinespace
 charged & $D_\eta$ & 0.458120 & 0.458119\\
         & $c_s$ & 0.448585 & 0.448585\\
         & $D_B$ & 0.736939 & 0.736949\\
         & $\Gamma_s$ & 0.392189 & 0.392189\\
 \bottomrule
 \end{tabular}
\end{table}

The agreement in table~\ref{tab:hydro} identifies all three poles without
using field-content heuristics.  In particular, the longitudinal diffusive
root is the charge/heat eigenmode of the complete thermodynamic matrix.  The
largest primitive constraint in the 48 roots is $9.1\times10^{-6}$ and the
largest source determinant is $5.6\times10^{-12}$.

\begin{figure}[t]
 \centering
 \includegraphics[width=\textwidth]{figures/hydrodynamic_critical_dispersion.pdf}
 \caption{Independent hydrodynamics versus full QNMs.  Open symbols are QNM
 roots and lines in panels (a,b) are predictions from the charged-fluid
 matrix, evaluated before any dispersion fit.  Color labels the neutral or
 charged state; squares show sound in (a), while circles, squares, and
 triangles distinguish shear, sound, and diffusion in (b).  Panel (c) shows
 the relative QNM--hydrodynamic difference for the same roots.  Panel (d)
 plots $D_B$ directly against $\chi_B/T^2$ along the hot stable
 $\mub=\mub^c$ approach; only the green segment is the displayed critical
 fit used to infer $z$.}
 \label{fig:hydro-critical}
\end{figure}

\section{Critical slowing at the endpoint}
\label{sec:critical}

We approach the MAP cusp on the hot stable branch at fixed
$\mub=\mub^c$.  The horizon coordinate is displaced by
$0.5\geq\phi_0^c-\phi_0\geq0.005$, and the electric datum is solved anew at
every point to keep $\mub$ fixed.  Define the dimensionless distance
\begin{equation}
 r=\left[\left(\frac{T-T_c}{T_c}\right)^2+
 \left(\frac{\mub-\mub^c}{\mub^c}\right)^2\right]^{1/2}.
 \label{eq:reduced-distance}
\end{equation}
Over the asymptotic window, $D_B\sim r^{0.64--0.66}$ and
$\chi_B/T^2\sim r^{-0.62--0.63}$.  With the classical holographic static
anomalous dimension $\eta_{\rm stat}=0$, dynamic scaling gives
\begin{equation}
 D_B\sim\xi^{2-z},\qquad \chi_B\sim\xi^2,\qquad
 z=2-2\frac{\dd\log D_B}{\dd\log\chi_B}.
 \label{eq:z-scaling}
\end{equation}
Varying the fit over the last four through eight points gives
\begin{equation}
 z=4.08\pm0.05\quad\text{(fit-window systematic)}.
 \label{eq:z-hydro}
\end{equation}
The local thermodynamic Jacobian condition number rises from $1.3\times10^2$
to $2.8\times10^6$ along this path, while the Maxwell-relation residual stays
below $3.7\times10^{-6}$.

At the cusp itself, the $q^2$ coefficient vanishes and the full QNM determines
the leading gradient power.  Seven momenta from $\q=0.04$ to 0.15 give
\begin{equation}
 -\operatorname{Im}\w_D=A\q^z,\qquad z=3.99\quad
 (0.04\leq\q\leq0.08),
 \label{eq:critical-q4}
\end{equation}
with local exponents decreasing from 3.996 to 3.919 as the upper end is moved
to $\q=0.15$.  We quote $z=3.99(2)$ for the direct dispersion, where the
parenthesis covers this controlled window drift.  The largest critical-QNM
constraint is $9.1\times10^{-7}$ and the source determinant is below
$1.7\times10^{-11}$.

\begin{figure}[t]
 \centering
 \includegraphics[width=\textwidth]{figures/cep_spinodal_dynamics.pdf}
 \caption{Critical and spinodal diffusive dynamics.  In (a), open circles are
 full QNMs at the MAP cusp, the solid green line is the fit over the shaded
 four-point window, and the gray dashed line is a $\q^4$ reference normalized
 at the lowest momentum.  The inset gives adjacent-point effective
 exponents.  In (b), markers are full QNMs near the hot fold, at the midpoint,
 and near the cold fold; solid curves are spline interpolations used only as
 guides, and dashed curves are independent signed charged-hydrodynamic
 predictions.  The shaded upper half-plane is unstable; the star and dotted
 line mark the MAP midpoint maximum and band edge.}
 \label{fig:cep-spinodal}
\end{figure}

The homogeneous modes do not close at the cusp.  The least-damped gapped
triplet has $\w=1.68854-0.10317\ii$, while the leading singlet and tensor
dampings are 0.2561 and 0.8669.  Critical slowing is therefore carried by the
conserved longitudinal mode, not by an accidental homogeneous gapped pole.
This distinction is consistent with earlier holographic studies of
nonhydrodynamic critical behavior \cite{Finazzo:2016psx,DeWolfe:2011ts}.

\section{Spinodal growth and hydrodynamic breakdown}
\label{sec:spinodal}

At $\mub=650$ MeV the MAP fixed-chemical-potential line has folds at
$\phi_0=3.18138$ and 3.88392.  We solve the longitudinal spectrum at fractional
positions 0.1, 0.5, and 0.9 between them.  In all cases the signed hydrodynamic
diffusion coefficient is negative, so
$\operatorname{Im}\w_D=-D_B\q^2>0$ at small momentum.

We audited this identification before constructing the unstable-band figure.
Nine backgrounds span stable points on both sides, near-fold points, and the
three interior locations; at each one the full charged-fluid matrix was
diagonalized with normalized projections in
$(\delta\epsilon,\delta\rhob,\delta u_z)$.  The sound pair remains stable and
$c_s^2>0$ throughout, whereas only the charge/heat eigenvalue changes sign.
Dedicated QNM continuation down to $\q=0.0025$ gives a median leading power
1.990, and a $\q^2+\q^4$ model is strongly preferred over a
$\q+\q^2$ model at all three interior backgrounds.  The leading
nonhydrodynamic longitudinal pole remains gapped.  Thus this charged EMD
spinodal differs from the familiar sound-driven neutral case
\cite{Janik:2015waa}: its carrier is the baryon/heat-diffusion branch, not
merely an arbitrary purely imaginary root.  Appendix~\ref{app:mode-audit}
records the complete audit.

\begin{table}[t]
 \centering
 \caption{Full-QNM unstable bands at $\mub=650$ MeV.  The fractional position
 runs from the hot to the cold fold.  $\q_{10\%}$ is the largest sampled
 momentum where the hydrodynamic growth rate agrees with the full QNM within
 10\%.}
 \label{tab:spinodal}
 \begin{tabular}{@{}crrrrrr@{}}
 \toprule
 fraction & $T$ [MeV] & $D_B$ & $\q_*$ & $\Gamma_*$ & $\q_{\rm edge}$ & $\q_{10\%}$\\
 \midrule
 0.10 & 98.786 & -0.01829 & 0.198 & $3.41\times10^{-4}$ & 0.286 & 0.05\\
 0.50 & 99.940 & -0.03895 & 0.283 & $1.414\times10^{-3}$ & 0.417 & 0.08\\
 0.90 & 100.924 & -0.01060 & 0.154 & $1.22\times10^{-4}$ & 0.221 & 0.05\\
 \bottomrule
 \end{tabular}
\end{table}

Table~\ref{tab:spinodal} demonstrates a closed unstable band rather than only
a thermodynamic sign change.  The central maximum occurs at
$\q_*=0.2826$ with $\Gamma_*=0.001414$ and the pole returns to the lower half
plane at $\q_{\rm edge}=0.4175$.  Shooting at $\q=0.28$ agrees with nominal
collocation within $1.5\times10^{-10}$ in $\w$; the refined collocation agrees
to displayed precision and has primitive constraint $3.3\times10^{-7}$.

The $q^2$ hydrodynamic approximation correctly gives the initial slope but
cannot close the band.  Its 10\% range ends at $\q=0.05$--0.08, well below both
the fastest-growing scale and the edge.  The maximum growth, preferred
momentum, and band edge all decrease toward either fold.  Three positions are
enough to establish this trend but not to claim a spinodal-boundary exponent;
we therefore do not fit one.

\section{Posterior robustness}
\label{sec:posterior}

The HDF5 posterior contains 1600 draws, of which 1589 have successful released
critical-point metadata.  Because these are already posterior draws, their
base weights are equal.  We standardize the 11 model parameters together with
the stored critical coordinates, retain principal components carrying 95\% of
the variance, and select 25 deterministic cluster medoids.  Each medoid is
weighted by its Voronoi population, and sample 74 is forced into the ensemble.
The resulting weights sum to one and represent all 1589 successful draws.

For each medoid, the cusp is independently located from the fold and
null-direction conditions rather than copied from metadata.  Adaptive
finite-difference steps from $(0.01,0.001)$ through $(0.03,0.003)$ are used
only to control the nearly singular map; the maximum accepted cusp-equation
residual is $1.8\times10^{-4}$.  Every medoid then follows the fixed-$\mub^c$
critical path and a fixed-$\mub^c+50$ MeV line through both folds.

\begin{table}[t]
 \centering
 \caption{Weighted 25-medoid posterior summary.  Intervals are the 2.5th and
 97.5th percentiles of the represented posterior.  The separate common
 $z$ fit-window systematic is approximately $\pm0.05$.}
 \label{tab:posterior}
 \begin{tabular}{@{}lrrr@{}}
 \toprule
 observable & 2.5\% & median & 97.5\%\\
 \midrule
 $T_c$ [MeV] & 101.48 & 103.88 & 105.75\\
 $\mub^c$ [MeV] & 572.43 & 591.59 & 622.48\\
 $z$ from $D_B$--$\chi_B$ scaling & 4.062 & 4.075 & 4.085\\
 $D_B$ distance exponent & 0.457 & 0.648 & 0.676\\
 $\Delta T_{\rm sp}$ [MeV] at $\mub^c+50$ MeV & 1.843 & 1.876 & 1.903\\
 midpoint $D_B$ in the spinodal & -0.03836 & -0.03457 & -0.03235\\
 \bottomrule
 \end{tabular}
\end{table}

\begin{figure}[t]
 \centering
 \includegraphics[width=\textwidth]{figures/posterior_uq.pdf}
 \caption{Controlled posterior propagation.  In every panel marker area is
 represented posterior weight and color is log likelihood; the star denotes
 the MAP realization and open diamonds denote weighted medians.  Panel (a)
 shows independently reconstructed CEPs.  Panel (b) orders medoids by $z$;
 vertical bars are per-medoid fit-window half-ranges, the green band is the
 weighted 95\% posterior interval, and gray dashed lines show the separate
 common $\pm0.05$ window systematic.  Panel (c) compares spinodal temperature
 width with signed midpoint diffusion at $\mub^c+50$ MeV.  All 25 medoids lie
 in the shaded $D_B<0$ region.}
 \label{fig:posterior}
\end{figure}

Figure~\ref{fig:posterior} and table~\ref{tab:posterior} show that the
posterior spread of the exponent is smaller than the scaling-window
systematic.  We therefore summarize the ensemble as $z=4.08\pm0.05$, not with
three-decimal precision.  The wider posterior interval of the distance
exponent reflects sensitivity to how the physical path approaches the local
scaling axes; the $D_B$--$\chi_B$ relation and direct MAP $q^4$ dispersion are
more stable.  All 25 medoids possess two folds and have negative midpoint
$D_B$, so the existence of spinodal growth is posterior-robust.  Detailed
$\q_*$ and $\Gamma_*$ values remain full-QNM results for the MAP realization;
we do not present them as posterior intervals.

\FloatBarrier
\section{Conclusions}
\label{sec:conclusions}

We have computed the coupled finite-momentum real-time spectrum of the
Bayesian-calibrated PHA EMD model.  Gauge-invariant source matrices, exact
residual-gauge quotienting, second-order horizon recurrences, primitive
constraint monitoring, and two independent radial integrations turn the
helicity-one and helicity-zero systems into controlled continuum QNM
calculations.

On stable backgrounds, the shear, sound, and baryon/heat-diffusion poles match
transport and thermodynamic coefficients computed without QNM input.  This
agreement identifies the longitudinal modes and verifies the charged-fluid
mixing.  The maximum relative difference in the dedicated $\q=0.05$
benchmarks is $1.7\times10^{-3}$, and extrapolated coefficients agree more
closely.

At the independently located MAP critical endpoint, the diffusion coefficient
vanishes while the baryon susceptibility diverges.  The hydrodynamic scaling
relation gives $z=4.08\pm0.05$, and the full critical QNM independently obeys
$-\operatorname{Im}\w_D\sim\q^{3.99}$ at small momentum.  Homogeneous gapped
modes retain finite damping; the conserved diffusive pole is the critical
degree of freedom.

Inside the $\mub=650$ MeV spinodal branch, diffusion crosses into the upper
half frequency plane and forms a closed unstable band.  At the midpoint its
fastest-growing scale is $\q_*=0.283$, its maximum dimensionless growth is
$1.41\times10^{-3}$, and its edge is $\q=0.417$.  Hydrodynamics controls only
the initial $q^2$ segment and cannot predict closure.  The band shrinks toward
both spinodal boundaries.  An explicit nine-background audit finds positive
$c_s^2$, a stable sound pair, $q^{1.990}$ diffusive growth, and a separated
nonhydrodynamic pole, establishing that this charged instability is carried
by baryon/heat diffusion rather than sound.

A weighted 25-medoid posterior analysis represents all successful released
draws and reconstructs every local cusp and spinodal pair independently.  It
gives a weighted 95\% range $4.062<z<4.085$ before the dominant common
fit-window error, and every medoid retains negative diffusion inside the
spinodal.  This justifies a Bayesian robustness claim for critical slowing
and the existence of spinodal growth, while MAP-only band scales are labeled
as such.

The interpretation remains subject to classical large $N_c$, strong coupling,
translational invariance, equilibrium backgrounds, and the bottom-up model
Ansatz.  The results characterize a lattice-constrained holographic model of
QCD, not QCD itself.  Natural extensions include correlator residues,
hydrodynamic/nonhydrodynamic pole collisions at complex momentum, nonlinear
spinodal evolution, and full-QNM propagation beyond the thermodynamic medoid
ensemble.

\acknowledgments
OpenAI Codex was used as an AI-assisted tool during preparation of this work
for code refactoring, workflow automation, construction of numerical and
symbolic validation scripts, literature-metadata checks, and language and
layout editing.  Its outputs were checked against the version-controlled
source, machine-readable results, cited primary literature, and independent
numerical tests.  Responsibility for the scientific content and the submitted
manuscript remains with the authors.

\appendix

\section{Linearized identities and degree counting}
\label{app:identities}

The generated perturbation artifact contains three evolution equations and
one primitive Einstein constraint in helicity one.  Helicity zero contains
seven evolution/characteristic equations and the unused primitive equations
$E_{rr}$, $E_{rv}$, $E_{rz}$, and $M_r$.  Direct substitution verifies the
linearized Bianchi and Maxwell identities on every background.

The residual helicity-one orbit is proportional to
$(H_{vx},H_{zx},a_x)=(-\sigma X,-pX,0)$, so three primitives minus one gauge
orbit give the two physical sources in eq.~\eqref{eq:h1-invariants}.  In
helicity zero the four residual functions obey
\begin{equation}
 T'=0,\qquad R'=-A'R-\sigma T,\qquad
 Z'=-p e^{-A}T,\qquad \lambda'=0.
\end{equation}
Seven regular primitives minus four gauge directions give the three sources
in eq.~\eqref{eq:h0-invariants}.  The validation suite also checks transverse
parity, charge conjugation, the neutral limit, the $k\to0$ triplet and singlet
reductions, and all UV source/vev powers.  All ten symbolic tests pass.

\section{Numerical error budget}
\label{app:error-budget}

The coupled validation table is stored row by row, including frequency,
source determinant and singular value, gauge defect, primitive constraints,
horizon-recurrence condition, local principal-matrix condition, collocation
residual, $r_0$, $r_{\rm UV}$, resolution, and optimizer status.  A root is
accepted by the source residual even when MINPACK reports stagnation, provided
that the independently evaluated determinant, continuum envelope, and radial
cross-check pass.  Optimizer status alone is never a physics criterion.

For the critical exponent, five nested windows containing the last four
through eight thermodynamic points give $4.044$, $4.057$, $4.079$, $4.107$,
and $4.142$.  Their half-range sets the quoted $\pm0.05$ systematic.  The
direct QNM fits over four through seven low-momentum points give exponents
3.987, 3.982, 3.976, and 3.968.  Posterior and fit-window uncertainties are
reported separately because they arise from different sources.

\section{Longitudinal mode-identity audit}
\label{app:mode-audit}

The charged hydrodynamic evolution matrix was evaluated on nine ordered
backgrounds across the $\mub=650$ MeV folds.  Its three right eigenvectors
were normalized in the relative variables
$(\delta\epsilon/(\epsilon+P),\delta\rhob/\rhob,\delta u_z)$ before their
composition was compared.  QNM roots were continued in background and in
momentum; the diffusive branch uses eight values from $\q=0.0025$ to 0.05 at
each of the three interior locations, while the sound pair is checked across
the complete background sequence.  Production roots are reused when
available and every new root is stored with its source and constraint
residuals.

\begin{figure}[t]
 \centering
 \includegraphics[width=\textwidth]{figures/longitudinal_mode_identity_audit.pdf}
 \caption{Audit of the unstable longitudinal branch.  Panel (a) follows the
 independently computed $c_s^2$ and signed $D_B$ across both folds; the sound
 coefficient stays positive while diffusion changes sign.  Panel (b) shows
 small-$\q$ diffusive QNMs at three interior backgrounds; open markers are
 roots and dashed curves are the displayed $\q^2+\q^4$ model comparisons.
 Panel (c) gives normalized hydrodynamic eigenvector projections at the
 midpoint.  Panel (d) places sound, diffusion, and the first $q=0$
 nonhydrodynamic longitudinal pole in the complex plane, demonstrating the
 latter's separation.  The complete machine-readable audit records fits,
 residuals, susceptibility diagnostics, and provenance.}
 \label{fig:mode-audit}
\end{figure}

The QNM leading powers are 1.990, 1.994, and 1.984 near the hot fold, at the
midpoint, and near the cold fold.  The $\q^2+\q^4$ model has a lower BIC than
models containing a leading linear term in all three cases.  This power
counting, the signed coefficient, background continuity, and stable sound
pair jointly determine the terminology used in the main text.
\FloatBarrier

\section{Background, CEP, and thermodynamic reproduction}
\label{app:thermodynamics}

\subsection{Exact kernels and background equations}

All model derivatives are evaluated analytically.  In particular,
\begin{align}
 V_\phi={}&-12\gamma\sinh(\gamma\phi)+2b_2\phi+4b_4\phi^3+6b_6\phi^5,\nonumber\\
 V_{\phi\phi}={}&-12\gamma^2\cosh(\gamma\phi)+2b_2+12b_4\phi^2+30b_6\phi^4,\\
 f_{\phi\phi}=\frac{1}{1+d_1}\bigg\{
 &\operatorname{sech}g\left[\tanh^2g-\operatorname{sech}^2g\right](g')^2
 -\operatorname{sech}g\tanh g\,g''\nonumber\\
 &+d_1d_2^2\operatorname{sech}(d_2\phi)
 \left[\tanh^2(d_2\phi)-\operatorname{sech}^2(d_2\phi)\right]\bigg\}.
 \label{eq:fpp}
\end{align}
Figure~\ref{fig:kernels} shows the exact MAP kernels.  The narrow structure in
$f(\phi)$ near the boundary, controlled by the large value of $d_2$, is
resolved rather than smoothed or removed.

\begin{figure}[t]
 \centering
 \includegraphics[width=\textwidth]{figures/pha_model_kernels.pdf}
 \caption{Exact MAP model kernels.  Panel (a) shows the dilaton potential and
 panel (b) the Maxwell coupling; the light blue region marks the horizon-field
 range sampled by the production phase surface.  The inset resolves the
 near-boundary coupling layer at $\phi\sim d_2^{-1}$.  Curves are direct
 evaluations of the analytic parametrization, not fits or interpolations.}
 \label{fig:kernels}
\end{figure}

\begin{table}[t]
 \centering
 \caption{Maximum-likelihood PHA realization.  Exact binary64 values and
 provenance hashes are retained in the machine-readable configuration.}
 \label{tab:parameters}
 \begin{tabular}{@{}lr@{}}
 \toprule
 quantity & value\\
 \midrule
 $\Lambda$ [MeV] & 1123.87\\
 $\kappa_5^2$ & 11.3705\\
 $\gamma$ & 0.590424\\
 $b_2$ & 0.327227\\
 $b_4$ & -0.0480134\\
 $b_6$ & 0.000938515\\
 $c_1$ & 0.00880048\\
 $c_2$ & 0.155799\\
 $c_3$ & 0.0528185\\
 $d_1$ & 1.71775\\
 $d_2$ & 1679.18\\
 $m^2$ & -3.52875\\
 $\Delta$ & 2.68648\\
 \bottomrule
 \end{tabular}
\end{table}

For the background ansatz in eq.~\eqref{eq:background}, the equations used by
the independent reference integrator are
\begin{align}
 \phi''+\left(4A'+\frac{h'}h\right)\phi'
 -\frac1h\left(V_\phi-\frac12e^{-2A}f_\phi\Phi'^2\right)&=0,\nonumber\\
 \Phi''+\left(2A'+\frac{f_\phi}{f}\phi'\right)\Phi'&=0,\nonumber\\
 A''+\frac16\phi'^2&=0,\qquad
 h''+4A'h'-e^{-2A}f\Phi'^2=0.
 \label{eq:background-eoms}
\end{align}
They obey the constraint and conserved charge
\begin{align}
 \mathcal C&=h(24A'^2-\phi'^2)+6A'h'+2V+e^{-2A}f\Phi'^2=0,\nonumber\\
 \mathcal Q&=e^{2A}f\Phi'=\text{constant}.
 \label{eq:diagnostics}
\end{align}
The reduced production path evolves $\mathcal Q$ directly and reconstructs
\begin{equation}
 \Phi'(r)=\frac{\mathcal Q e^{-2A(r)}}{f[\phi(r)]}.
 \label{eq:reduced-charge}
\end{equation}

\subsection{Regular horizon series and background diagnostics}
\label{app:horizon}

Writing
\begin{align}
 A&=A_1r+A_2r^2+\cdots,&h&=r+h_2r^2+\cdots,\nonumber\\
 \phi&=\phi_0+\phi_1r+\phi_2r^2+\cdots,&
 \Phi&=\Phi_1r+\Phi_2r^2+\cdots,
\end{align}
the compiled recurrence reproduces
\begin{align}
 A_1&=-\frac{2V_0+f_0\Phi_1^2}{6},&
 \phi_1&=V_{\phi,0}-\frac12f_{\phi,0}\Phi_1^2,\nonumber\\
 A_2&=-\frac{\phi_1^2}{12},&
 h_2&=\frac12(f_0\Phi_1^2-4A_1),\nonumber\\
 \Phi_2&=-\frac{\Phi_1}{2}\left(2A_1+
 \frac{f_{\phi,0}}{f_0}\phi_1\right).
 \label{eq:horizon-series}
\end{align}
Higher coefficients through fourth order are generated by truncated-series
arithmetic applied to the regular equations, rather than hand-entered
formulae.

The conserved-charge background system is integrated from
$\epsilon_H=10^{-7}$ to $r_{\rm max}=12$ with DOP853, relative tolerance
$3\times10^{-12}$, absolute tolerance $3\times10^{-14}$, and maximum step
0.025.  UV coefficients are extracted on a fixed 2400-point grid.  Across the
90-point validation scan, the normalized Einstein constraint is at most
$3.50\times10^{-9}$ and Gauss drift is below $4.45\times10^{-16}$; at the MAP
cusp they are $1.88\times10^{-11}$ and $3.33\times10^{-16}$, respectively.

\begin{figure}[t]
 \centering
 \includegraphics[width=\textwidth]{figures/representative_background.pdf}
 \caption{MAP critical background used by the homogeneous and coupled
 benchmarks.  Panel (a) shows normalized metric functions.  Panel (b) shows
 scalar decay and the Maxwell-coupling boundary layer, with an inset resolving
 its UV tail.  Panel (c) displays the pointwise relative electric-flux error
 and its local maximum envelope rather than the nearly constant flux itself;
 the dashed line is the independently stored Gauss-drift diagnostic.  The
 regular profiles and small relative error test
 the background input without relying on a QNM calculation.}
 \label{fig:background}
\end{figure}

Figure~\ref{fig:background-grid-map} displays the complete production grid in
horizon coordinates and its thermodynamic image.  It replaces the earlier
90-background exploratory survey: the latter remains a repository artifact
but is not reader-facing evidence.  The fold locus makes explicit why
isolated inversion in $(T,\mub)$ is ill-conditioned and why all dynamical
backgrounds are selected by continuation in horizon data.

\begin{figure}[t]
 \centering
 \includegraphics[width=\textwidth]{figures/background_grid_map.pdf}
 \caption{Production background grid and thermodynamic map.  Panel (a) shows
 successful horizon-data points as a light rasterized cloud, failed points as
 gray crosses, selected fixed-$\mub$ contours in gray, the $\det J=0$ fold
 locus in vermillion, the CEP, and the three spinodal QNM backgrounds.  Panel
 (b) maps representative constant-$\phi_0$ and constant-charge families into
 $(\mub,T)$, together with folds, coexistence, and the same dynamical points.
 Only the dense point clouds are rasterized; curves and text remain vector.
 The overlapping families visualize the multivalued inverse map that
 motivates branch continuation.}
 \label{fig:background-grid-map}
\end{figure}

\subsection{Independent critical-point reproduction}

The public reference-compatible construction follows crossings of neighboring
constant-$\phi_0$ curves.  Increasing the number of trajectories from 100 to
800 moves the estimate from $(103.666,594.761)$ MeV to
$(103.761,593.384)$ MeV.  The independent local method uses
\begin{equation}
 J=\frac{\partial(\log T,\log\mu_B)}
 {\partial(\phi_0,\Phi_1/\Phi_1^{\rm max})}
 \label{eq:thermo-jacobian}
\end{equation}
and imposes both $\det J=0$ and a vanishing directional derivative along the
null vector of $J$.  Table~\ref{tab:cep} makes the provenance discrepancy
explicit.

\begin{table}[t]
 \centering
 \caption{Critical coordinates from posterior metadata and two independent
 local reconstructions.  The two numerical routes agree with one another;
 the distinct metadata value is not used to choose the dynamical background.}
 \label{tab:cep}
 \begin{tabular}{@{}lrr@{}}
 \toprule
 method & $T_c$ [MeV] & $\mu_B^c$ [MeV]\\
 \midrule
 HDF5 metadata & 103.8978 & 602.4749\\
 constant-$\phi_0$ crossings ($N=800$) & 103.7611 & 593.3845\\
 local cusp equations & 103.7553 & 593.3227\\
 \bottomrule
 \end{tabular}
\end{table}

\begin{figure}[t]
 \centering
 \includegraphics[width=0.82\textwidth]{figures/cep_reproduction.pdf}
 \caption{Convergence of the reference-compatible constant-$\phi_0$
 crossing algorithm, shown as differences from the independent local cusp.
 Circles are crossing calculations at increasing line count, green dashed
 lines are zero difference, and gray dotted lines are the fixed offsets of
 the HDF5 metadata.  Both reconstructed coordinates approach the local cusp,
 while the metadata remain distinct; the plot shows no fitted convergence
 order.}
 \label{fig:cep-reproduction}
\end{figure}
\FloatBarrier

\subsection{Neutral equation of state and baryon susceptibility}

As a background test independent of every QNM solver, we reproduce the neutral
entropy and the second-order baryon susceptibility
\begin{equation}
 \chi_2^B(T)=\left.\frac{\partial(\rho_B/T^3)}
 {\partial(\mu_B/T)}\right|_{\mu_B=0}.
 \label{eq:chi2}
\end{equation}
For each of 30 neutral backgrounds, the centered derivative is
\begin{equation}
 \chi_2^B(\delta)=
 \frac{(\rho_B/T^3)_{+\delta}-(\rho_B/T^3)_{-\delta}}
 { (\mu_B/T)_{+\delta}-(\mu_B/T)_{-\delta}},
 \qquad \delta=0.005,0.010,0.020.
 \label{eq:chi2fd}
\end{equation}
Charge conjugation makes $T$ and $s$ even and $\mu_B$ and $\rho_B$ odd in
$\delta$, so fixing $\phi_0$ differs from fixing $T$ only at
$\order(\delta^2)$.  The 90 evaluations span
$74.69\leq T/{\rm MeV}\leq1003.70$; the largest susceptibility displacement
relative to the finest step is $2.06\times10^{-3}$ and occurs at the coarsest
step.  No smoothing or fitted interpolation enters the displayed points.

\begin{figure}[!htbp]
 \centering
 \includegraphics[width=\textwidth]{figures/thermodynamic_validation.pdf}
 \caption{Reproduction and step-size stability of neutral thermodynamics.
 Panel (a) shows $s/T^3$.  Panel (b) shows $\chi_2^B$ from
 eq.~\eqref{eq:chi2fd}; color, marker, and line style redundantly encode the
 three charge steps.  Panel (c) gives the relative displacement from the
 finest step in percent, rather than mixing absolute and relative scales.
 Symbols are calculated backgrounds and connecting segments only guide the
 eye.  This equilibrium validation is independent of the finite-density
 critical and spinodal claims.}
 \label{fig:thermodynamic-validation}
\end{figure}
\FloatBarrier

\section{Homogeneous gapped spectrum and legacy benchmark}
\label{app:homogeneous}

\subsection{Homogeneous master equations}

At $k=0$, $SO(3)$ organizes the perturbations into quintuplet, triplet, and
singlet sectors.  The decoupled tensor equation (and finite-$k$ helicity-two
benchmark) is
\begin{equation}
 Z_2''+\left(4A'+\frac{h'}h-\frac{2\ii\omega e^{-A}}h\right)Z_2'
 +\left(-\frac{e^{-2A}k^2}{h}-\frac{3\ii\omega e^{-A}A'}h\right)Z_2=0.
 \label{eq:tensor}
\end{equation}
The homogeneous vector master field obeys
\begin{multline}
 a''+\left(2A'+\frac{h'}h+\frac{f_\phi}{f}\phi'
 -\frac{2\ii\omega e^{-A}}h\right)a'\\
 +\left[-\frac{e^{-2A}f\Phi'^2}{h}
 -\frac{\ii\omega e^{-A}}h\left(A'+\frac{f_\phi}{f}\phi'\right)\right]a=0.
 \label{eq:vector}
\end{multline}
Their normalizable powers are $Z_2\sim u^4$ and $a\sim u^2$ for
$u=e^{-A}$.  For the singlet, define
\begin{equation}
 {\cal S}=\varphi-\frac{\phi'}{2A'}
 \frac{H_{xx}+H_{yy}+H_{zz}}{3},\qquad
 Y=\frac{\phi'}{A'},\qquad \psi=\frac{{\cal S}}Y.
 \label{eq:singlet-invariant}
\end{equation}
The source-free homogeneous master equation is
\begin{multline}
 \psi''+\left(4A'+\frac{h'}h+2\frac{Y'}Y
 -\frac{2\ii\omega e^{-A}}h\right)\psi'\\
 +\left[-\frac{h'}h\frac{Y'}Y
 +\frac{e^{-2A}\Phi'^2}{h\phi'}(3A'f_\phi-f\phi')
 -\frac{\ii\omega e^{-A}}h\left(3A'+2\frac{Y'}Y\right)\right]\psi=0.
 \label{eq:singlet}
\end{multline}
The first-order symbolic variation of the complete EMD system reproduces
eqs.~\eqref{eq:vector} and \eqref{eq:singlet}.  After UV source factoring,
the collocation problem is the frequency-linear pencil
\begin{equation}
 [M_0(k)+\omega M_1(k)]\bm v=0,
 \label{eq:pencil}
\end{equation}
with the numerical conversion
\begin{equation}
 \w=2\omega_{\rm num},\qquad
 k_{\rm num}=\frac{\q}{2\sqrt{h_0^{\rm far}}}.
 \label{eq:rescaling}
\end{equation}

\subsection{Continuum validation at the MAP cusp}

The homogeneous quintuplet, triplet, and singlet operators factor the UV
sources analytically.  Resolution and domain errors are varied separately:
$N=64,\ldots,160$ at $r_{\rm cut}=10$, and $r_{\rm cut}=8,\ldots,12$ at
$N=160$.  Right-eigenfunction overlaps track each branch, while left and
right null vectors give
\begin{equation}
 \kappa_\omega=\frac{\sqrt{1+|\omega|^2}\,\|u\|\,\|v\|}
 {|u^\dagger M_1v|}.
 \label{eq:homogeneous-conditioning}
\end{equation}
The largest condition estimate among accepted modes is $2.1\times10^5$.
An independent complex shooting solver varies both the horizon start and UV
extraction radius; no inverse of the generalized pencil is formed
\cite{Jansen:2017oag}.

\begin{table}[t]
 \centering
 \caption{Independently validated homogeneous poles at the MAP cusp.
 Parentheses give the conservative spectral/shooting envelope on the last
 displayed digits.}
 \label{tab:poles}
 \begin{tabular}{@{}cclcc@{}}
 \toprule
 sector & $n$ & $\mathfrak w_n$ & source residual & $\kappa_\omega$\\
 \midrule
 quintuplet & 0 & $1.8732(3)-0.8669(6)i$ & $9.5\times10^{-15}$ & $2.1\times10^5$\\
 triplet & 0 & $1.688542(4)-0.103168(6)i$ & $3.9\times10^{-14}$ & $4.6\times10^2$\\
 triplet & 1 & $2.8976(34)-0.8634(12)i$ & $9.6\times10^{-14}$ & $1.4\times10^4$\\
 singlet & 0 & $1.4924(12)-0.2561(13)i$ & $9.3\times10^{-12}$ & $5.1\times10^2$\\
 \bottomrule
 \end{tabular}
\end{table}

The leading triplet is oscillatory and gapped; its small damping is not a
baryon-diffusion pole.  Three former discrete-pencil roots fail the independent
source test and are excluded.  Figure~\ref{fig:convergence} retains the full
continuum evidence for the four accepted modes.

\begin{figure}[t]
 \centering
 \includegraphics[width=\textwidth]{figures/qnm_convergence.pdf}
 \caption{Continuum checks for the four accepted homogeneous poles.  Markers
 give distances from the complex-shooting root; panel (a) varies radial
 resolution at fixed UV domain and panel (b) varies the UV cutoff at fixed
 $N=160$, so distinct studies are not connected into a single sequence.  The
 light green region is the accepted numerical envelope and the gray dashed
 line marks the shooting root floor used for visual scale.  Circles denote
 resolution variation and squares denote UV-domain variation; color identifies
 the four accepted modes.}
 \label{fig:convergence}
\end{figure}

\subsection{Controlled thermodynamic trajectories}

The leading pole in each sector is continued through 18 backgrounds on each
of the physical curves $\mub=0$ and $\mub/T=2$, spanning approximately
$87\leq T\leq346$ MeV.  Every point is a shooting root; the associated
factored-collocation eigenfunction is assigned using frequency and overlap
with its parent.  The minimum parent overlap is 0.9985 and the largest
pointwise shooting/spectral difference is 0.012 in $\mathfrak w$.

The quintuplet and singlet damping rates are nonmonotonic but remain of order
unity or a few tenths.  The leading triplet changes more strongly: toward the
low-temperature endpoint, $-\operatorname{Im}\mathfrak w$ falls to
$3.7\times10^{-3}$ while the real part remains nonzero.  A dedicated endpoint
study through $N=160$ reproduces this damping within $2\times10^{-7}$.

\begin{figure}[t]
 \centering
 \includegraphics[width=\textwidth]{figures/homogeneous_qnm_trajectories.pdf}
 \caption{Validated leading homogeneous poles on the physical trajectories
 $\mub=0$ (solid) and $\mub/T=2$ (dashed).  Color identifies the quintuplet,
 triplet, or singlet, and symbols are calculated continuation points.  Panels
 (a,b) show the real part and damping versus temperature; panel (c) shows the
 complex-plane paths, with arrows only there indicating increasing
 temperature.  Panel (d) converts damping to a linear relaxation time.  The
 triplet becomes long lived toward the low-temperature endpoint, but panel
 (a) simultaneously shows that its real part remains finite: it is gapped and
 is not the conserved critical pole.}
 \label{fig:physical-trajectories}
\end{figure}
\FloatBarrier

\subsection{Comparison with the 2018 EMD realization}
\label{sec:old-emd-comparison}

To separate solver performance from model dependence, we replace the PHA
potential, Maxwell coupling, and scale by the functions and parameters of
ref.~\cite{Rougemont:2018taa} without changing either numerical algorithm.
A local reconstruction gives its endpoint at $(89.122,723.551)$ MeV,
consistent with the rounded $(89,724)$ MeV quoted there.  At this endpoint and
at two common neutral states, all nine published relaxation times are
reproduced within $0.0111$ fm/$c$; the maximum shooting source residual is
$7.5\times10^{-9}$ and the largest spectral/shooting distance is 0.0074 in
$\mathfrak w$.

\begin{table}[!htbp]
 \centering
 \caption{Linear relaxation times in the 2018 EMD realization and the PHA
 MAP model.  ``Published'' denotes the approximate values quoted in
 ref.~\cite{Rougemont:2018taa}; endpoint rows use each model's own CEP.}
 \label{tab:old-emd-comparison}
 \begin{tabular}{@{}llrrrrr@{}}
 \toprule
 state & model & $T$ & $\mu_B$ & $\tau_5$ & $\tau_3$ & $\tau_1$\\
 & & \multicolumn{2}{c}{[MeV]} & \multicolumn{3}{c}{$[\mathrm{fm}/c]$}\\
 \midrule
 high $T$ & 2018 published  & 400 & 0 & 0.060 & 0.080 & 0.110\\
          & 2018 reproduced & 400 & 0 & 0.059 & 0.080 & 0.108\\
          & PHA MAP         & 400 & 0 & 0.059 & 0.081 & 0.112\\
 \addlinespace
 neutral crossover & 2018 published  & 145 & 0 & 0.260 & 1.000 & 0.700\\
          & 2018 reproduced & 145 & 0 & 0.261 & 1.008 & 0.703\\
          & PHA MAP         & 145 & 0 & 0.261 & 1.025 & 0.634\\
 \addlinespace
 model CEP & 2018 published  & 89.0 & 724.0 & 0.330 & 2.150 & 1.260\\
          & 2018 reproduced & 89.1 & 723.6 & 0.329 & 2.139 & 1.252\\
          & PHA MAP         & 103.8 & 593.3 & 0.349 & 2.934 & 1.182\\
 \bottomrule
 \end{tabular}
\end{table}

\begin{figure}[t]
 \centering
 \includegraphics[width=\textwidth]{figures/legacy_2018_qnm_curves.pdf}
 \caption{Independent numerical reproduction using the 2018 EMD
 parametrization.  Rows show the quintuplet, triplet, and singlet; columns
 show $\operatorname{Re}\w$ and $-\operatorname{Im}\w$ versus temperature at
 five fixed chemical potentials.  Open markers are source-factored
 collocation calculations and thin segments are piecewise-linear guides to
 the eye, not fitted or digitized published curves.  Sixty-five backgrounds
 give 195 displayed QNM points.  Independent shooting at the nine landmarks
 of table~\ref{tab:old-emd-comparison} supplies the quantitative cross-check.
 The reproduction validates the shared solver for a known parametrization; it
 does not imply universality between that model and PHA.}
 \label{fig:legacy-curves}
\end{figure}

At $T=400$ MeV the two models differ by at most 0.004 fm/$c$.  At 145 MeV the
PHA singlet relaxes about ten percent faster.  At their respective endpoints
both models have $\tau_3>\tau_1>\tau_5$, while the PHA triplet time is about
37 percent larger.  This is a cross-model response comparison, not a
universality or critical-exponent statement.  These homogeneous results
validate the common background/spectral infrastructure but are not used to
infer diffusion or spinodal growth.
\FloatBarrier

\section{Reproducibility and data products}
\label{app:repro}

The complete Route-A analysis is regenerated from the repository root by
\begin{verbatim}
python -m pip install -r analysis/requirements.txt
python analysis/reproduce_reference_cep.py --lines 800 --neighbors-only \
  --output results/python/reference_cep_neighbors_N800.json
python analysis/locate_reference_cusp.py --phi-step 0.01 \
  --charge-step 0.001 --uv-tolerance 1e-5 \
  --output results/python/reference_cusp_uv1e5_h10.json
python analysis/build_phase_diagram.py --n-phi 141 --n-charge 121 \
  --phi-min 2.2 --phi-max 6.5 --charge-min 0.02 --charge-max 0.62 \
  --mu-min 594 --mu-max 850 --mu-count 65
python analysis/validate_phase_diagram.py
python analysis/plot_cep_reproduction.py
python analysis/run_numerics.py
python derivations/generate_linearized_emd.py
python derivations/validate_linearized_emd.py
python analysis/validate_decoupled_qnms.py
python analysis/build_homogeneous_trajectories.py
python analysis/compare_2018_emd.py
python analysis/plot_qnm_validation.py
python analysis/plot_homogeneous_trajectories.py
python analysis/validate_coupled_qnms.py
python analysis/run_finite_k_physics.py --stage all
python analysis/audit_longitudinal_mode_identity.py
python analysis/run_posterior_uq.py --samples 25 --workers 4
python analysis/plot_legacy_2018_curves.py
python analysis/render_all_figures.py --gallery --check
\end{verbatim}
The posterior driver contains its adaptive cusp fallback and therefore does
not require manual sample removal or retuning.  The run uses the
checksum-recorded source HDF5 and selects sample 74 with
$\log\mathcal L=31.0751$.  The primary Route-A outputs are
\path{coupled_qnm_validation.csv},
\path{finite_k_hydrodynamic_dispersion.csv},
\path{cep_critical_scaling.csv},
\path{cep_q4_dispersion.csv},
\path{spinodal_dispersion.csv}, and
\path{posterior_uq_samples.json}, with JSON summaries storing fits and
acceptance thresholds.  The mode-identity audit is stored in
\path{longitudinal_mode_identity_audit.csv/json}, and the 2018-model curves in
\path{legacy_2018_qnm_curves.csv/json}.  The figure manifest records every
input, semantic layer, fit window, interpolation role, and output.  The
finite-momentum scans are checkpointed after every root.

The retained validation products include
\path{thermodynamic_validation.csv} and the validated-mode,
homogeneous-trajectory, and 2018-benchmark CSV files.  Their JSON sidecars
store the horizon and
UV settings, branch labels, source residuals, condition estimates, tracking
parents, eigenfunction overlaps, and independent-method envelopes.  Thus the
appendices are regenerated data products, not historical prose detached from
the current pipeline.

The source HDF5 checksum and exact sample attributes are recorded in the data
manifest.  Source code and machine-readable outputs are available at
\url{https://github.com/RenatoPhys/pha-qnm}; the upstream posterior data are
archived with ref.~\cite{HippertZenodo}.  The author-controlled identity,
funding, arXiv, and persistent-release metadata must be supplied before
submission.

\bibliographystyle{JHEP}
\bibliography{references}

\end{document}
